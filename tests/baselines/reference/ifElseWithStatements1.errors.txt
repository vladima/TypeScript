==== tests/cases/compiler/ifElseWithStatements1.ts (4 errors) ====
    if (true)
        f();
        ~
!!! Cannot find name 'f'.
    else
        f();
        ~
!!! Cannot find name 'f'.
        ~~~~
!!! Unreachable code detected.
    
    function foo(): boolean {
        if (true)
            return true;
        else
            return false;
            ~~~~~~~~~~~~~
!!! Unreachable code detected.
    }
    